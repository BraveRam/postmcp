{
  "name": "postmcp-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "PostMCP - The Postman for MCP. Turn any OpenAPI spec into a context-optimized MCP server in seconds.",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "dev": "pnpm -r run dev",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.17.0"
}
